package com.ExceptionHandling.example;

public class UnCheckedExceptionClass {

	public static void main(String[] args) {
		//throw new Exception(); //compile time error because its checked exception and compiler  checked itself or handle not programmer
	}
}
