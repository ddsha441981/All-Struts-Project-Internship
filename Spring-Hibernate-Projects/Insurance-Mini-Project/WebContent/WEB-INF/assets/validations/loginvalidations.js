function loginsValidations()
{
	//alert("Login Validations method Call");
	if(emailValidate() & passwordValidate()/*  & selectMyRole() */)
	{
	
	return true;
	}
	else
	{
		//alert("No Email,Password Validate Functions Called");
	return false;
	}
	
	function emailValidate()
	{
		//alert("Yes Email Validate Functions Called");
		
		var x = document.forms["myForm"]["email_id"].value;
	    var atpos = x.indexOf("@");
	    var dotpos = x.lastIndexOf(".");
	    if(x==""){
	    	document.getElementById("emailError").innerHTML="Email must not be empty";
	    	
	    	return false;
	    }
	    if (atpos<1 || dotpos<atpos+2 || dotpos+2>=x.length) {
	    	document.getElementById("email_id").focus();
	        document.getElementById("emailError").innerHTML="Enter a email followed by @";
	        
	        return false;
	    }
	    else{
	    	document.getElementById("emailError").innerHTML="<span class='glyphicon glyphicon-ok'></span>";
	    	
	    	return true;
	    }	
	}
	
	function passwordValidate()
	{
		alert("Yes Password Validate Functions Called");	
		
		
	
	
		var pass=document.getElementById("autogeneratedPwd").value;

		if(pass==""){
			document.getElementById("passwordError").innerHTML="Password must not be empty";
			
			return false;
		}
		else if((pass.length < 2) || (pass.length > 15)){
			document.getElementById("autogeneratedPwd").focus();
			document.getElementById("passwordError").innerHTML="Password must be between 2 to 10 characters";
		
			return false;
		}
		
		else{
			document.getElementById("passwordError").innerHTML="<span class='glyphicon glyphicon-ok'></span>";
			return true;
		}
	}
	
	/* function selectMyRole()
	{
		var listBoxSelection=document.forms["myForm"]["select"].value;
		if(listBoxSelection==0)
		{
		alert("select some data");
		return false;
		}
	} */
}
